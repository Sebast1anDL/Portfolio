---
---

<header class="bg-white/80 backdrop-blur-md shadow-sm border-b border-gray-100 fixed w-full top-0 z-50">
  <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center py-4">
      <!-- Logo -->
      <div class="flex-shrink-0">
        <div class="flex items-center space-x-3">
          <div class="relative">
            <img src="/Logo1.png" alt="SDL Logo" class="h-10 w-10 rounded-xl shadow-lg">
            <div class="absolute -inset-0.5 bg-gradient-to-r from-blue-500 to-purple-500 rounded-xl blur opacity-30"></div>
          </div>
          <span class="font-bold text-xl bg-gradient-to-r from-gray-800 to-gray-600 bg-clip-text text-transparent">SDL</span>
        </div>
      </div>
      
      <!-- Navigation -->
      <div class="flex items-center space-x-1">
        <a href="#sobre-mi" class="nav-link text-xs sm:text-sm">Sobre mí</a>
        <a href="#proyectos" class="nav-link text-xs sm:text-sm">Proyectos</a>
      </div>


    </div>
  </nav>
</header>

<style>
  .nav-link {
    @apply relative px-4 py-2 rounded-lg text-gray-600 hover:text-blue-600 font-medium transition-all duration-200;
  }
  
  .nav-link:hover {
    @apply bg-blue-50;
    transform: translateY(-1px);
  }
  
  .nav-link::after {
    content: '';
    @apply absolute bottom-0 left-1/2 w-0 h-0.5 bg-gradient-to-r from-blue-500 to-purple-500 transition-all duration-300;
    transform: translateX(-50%);
  }
  
  .nav-link:hover::after {
    @apply w-3/4;
  }
</style>